# ⚡ Быстрый деплой на VPS

## 🎯 Быстрый старт (5 минут)

### Шаг 1: Подключитесь к серверу

**Windows (PowerShell):**
```powershell
ssh root@45.92.173.37
# Пароль: GAbZDK&JgQ8$hpmk
```

**Или используйте WinSCP:**
- Хост: `45.92.173.37`
- Порт: `22`
- Пользователь: `root`
- Пароль: `GAbZDK&JgQ8$hpmk`

### Шаг 2: Загрузите проект на сервер

**Вариант A: Через WinSCP**
1. Подключитесь через WinSCP
2. Перетащите всю папку `football_school` в `/opt/`

**Вариант B: Через SCP (PowerShell)**
```powershell
scp -r C:\Users\LOQ\Desktop\App\CAM\football_school root@45.92.173.37:/opt/
```

### Шаг 3: Запустите автоматическую установку

На сервере выполните:

```bash
cd /opt/football_school
chmod +x setup_vps.sh
./setup_vps.sh
```

Этот скрипт автоматически:
- ✅ Установит все зависимости
- ✅ Настроит виртуальное окружение
- ✅ Настроит systemd service
- ✅ Настроит Nginx
- ✅ Настроит firewall

### Шаг 4: Инициализируйте базу данных

```bash
cd /opt/football_school
source venv/bin/activate
python init_db.py
```

### Шаг 5: Запустите приложение

```bash
systemctl start football_school
systemctl restart nginx
```

### Шаг 6: Проверьте работу

Откройте в браузере:
```
http://45.92.173.37
```

**Вход:**
- Логин: `admin`
- Пароль: `admin123`

## 🔍 Проверка статуса

```bash
# Статус приложения
systemctl status football_school

# Логи приложения
tail -f /var/log/football_school/error.log

# Логи nginx
tail -f /var/log/nginx/football_school_error.log
```

## 🔄 Обновление приложения

```bash
cd /opt/football_school
# Загрузите новые файлы через WinSCP или git pull
source venv/bin/activate
pip install -r requirements.txt
systemctl restart football_school
```

## 📚 Подробная инструкция

См. файл `DEPLOY_VPS.md` для детальной информации.

