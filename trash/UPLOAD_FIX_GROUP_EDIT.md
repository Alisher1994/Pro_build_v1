# 📤 Инструкция по загрузке исправления редактирования группы

## 🐛 Исправленная ошибка:
**400 (BAD REQUEST) при редактировании группы**

## ✅ Что было исправлено:

### 1. **Frontend (`frontend/static/js/groups.js`):**
- ✅ Добавлена валидация и обработка `schedule_days` с проверкой на пустые/невалидные значения
- ✅ Исправлена обработка `max_students` - теперь правильно преобразуется в число или null
- ✅ Исправлена обработка `late_threshold` - теперь правильно преобразуется в число
- ✅ Улучшена обработка ошибок - теперь показывается конкретное сообщение об ошибке от сервера
- ✅ Добавлена проверка статуса ответа перед парсингом JSON

### 2. **Backend (`app.py`):**
- ✅ Добавлена валидация `schedule_time` с обработкой пустых/невалидных значений
- ✅ Улучшена обработка `max_students` - теперь корректно обрабатывает пустые строки
- ✅ Добавлена валидация `late_threshold` с обработкой ошибок
- ✅ Улучшены сообщения об ошибках - теперь возвращаются понятные сообщения

---

## 🚀 Способ 1: Автоматическая загрузка (PowerShell)

### Windows:
```powershell
# Откройте PowerShell в папке проекта
cd "C:\Users\LOQ\Desktop\App\CAM\football_school"

# Загрузите файлы
scp frontend\static\js\groups.js root@45.92.173.37:/opt/football_school/frontend/static/js/groups.js
scp app.py root@45.92.173.37:/opt/football_school/app.py
```

**Пароль:** `12345678`

---

## 🚀 Способ 2: Ручная загрузка через WinSCP

1. **Откройте WinSCP**
2. **Подключитесь к серверу:**
   - Хост: `45.92.173.37`
   - Пользователь: `root`
   - Пароль: `12345678`
   - Порт: `22`

3. **Загрузите файлы:**
   - `frontend/static/js/groups.js` → `/opt/football_school/frontend/static/js/groups.js`
   - `app.py` → `/opt/football_school/app.py`

---

## 🔧 После загрузки на сервере:

### Подключитесь по SSH:
```bash
ssh root@45.92.173.37
```

### Выполните команды:
```bash
# Переход в директорию проекта
cd /opt/football_school

# Установка прав доступа (если нужно)
chown -R www-data:www-data frontend/static/js/groups.js
chown -R www-data:www-data app.py
chmod 644 frontend/static/js/groups.js
chmod 644 app.py

# Перезапуск сервиса
systemctl restart football_school

# Проверка статуса
systemctl status football_school --no-pager -l
```

### Проверка логов (если есть ошибки):
```bash
journalctl -u football_school -n 50 --no-pager
```

---

## ✅ Проверка работы:

1. **Откройте страницу групп:**
   - `https://d-promo.uz/groups`

2. **Проверьте редактирование группы:**
   - Нажмите кнопку "Редактировать" у любой группы
   - Измените любые параметры (название, время, дни, блоки стадиона)
   - Нажмите "Сохранить"
   - Группа должна успешно обновиться без ошибок 400

3. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку Console
   - Не должно быть ошибок `400 (BAD REQUEST)`
   - Не должно быть ошибок парсинга JSON

---

## 📋 Файлы для загрузки:

**Два файла:**
1. `frontend/static/js/groups.js` - исправления валидации на клиенте
2. `app.py` - исправления валидации на сервере

---

## 🐛 Если что-то пошло не так:

1. **Проверьте подключение к серверу:**
   ```bash
   ssh root@45.92.173.37
   ```

2. **Проверьте права доступа:**
   ```bash
   ls -la /opt/football_school/frontend/static/js/groups.js
   ls -la /opt/football_school/app.py
   ```

3. **Проверьте логи сервиса:**
   ```bash
   journalctl -u football_school -n 100 --no-pager
   ```

4. **Перезапустите Nginx:**
   ```bash
   systemctl reload nginx
   ```

5. **Очистите кэш браузера:**
   - Нажмите `Ctrl + Shift + R` (жесткая перезагрузка)
   - Или откройте в режиме инкогнито

---

## 📝 Примечания:

- Исправлены проблемы с валидацией данных при редактировании группы
- Улучшена обработка ошибок на клиенте и сервере
- После загрузки обязательно перезапустите сервис `football_school`
- Очистите кэш браузера для применения изменений JavaScript

---

## 🔍 Детали исправлений:

### JavaScript (groups.js):
- Добавлен try-catch для парсинга `schedule_days`
- Проверка на пустые значения для `max_students` и `late_threshold`
- Правильное преобразование типов перед отправкой
- Улучшенная обработка ошибок HTTP

### Python (app.py):
- Валидация формата времени с обработкой ошибок
- Правильная обработка пустых значений для `max_students`
- Валидация числовых полей с понятными сообщениями об ошибках

