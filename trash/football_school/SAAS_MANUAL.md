# Руководство по использованию SaaS-системы управления школами

## Обзор

Система представляет собой мультитенантную SaaS-платформу для управления футбольными школами. Каждая школа работает в полностью изолированном пространстве, не видя данные других школ.

## Роли пользователей

### 1. Супер-администратор (super_admin)
- Владелец платформы
- Управляет всеми школами (клиентами)
- Может создавать, редактировать, блокировать и удалять школы
- Видит все данные всех школ (для администрирования)
- Не привязан к конкретной школе

### 2. Администратор школы (admin)
- Владелец конкретной школы
- Управляет своей школой: учениками, группами, финансами и т.д.
- Не видит данные других школ
- Привязан к своей школе через `school_id`

### 3. Другие роли
- `financier` - финансист школы
- `payment_admin` - администратор платежей
- `teacher` - преподаватель

## Создание супер-администратора

Перед началом работы необходимо создать супер-администратора:

```bash
# Интерактивный режим (запрашивает логин и пароль)
python create_super_admin.py

# Автоматический режим (создает superadmin / superadmin123)
python create_super_admin_auto.py
```

**Важно:** После первого входа измените пароль!

## Управление школами

### Вход в систему как супер-администратор

1. Откройте страницу входа: `/login`
2. Введите логин и пароль супер-администратора
3. После входа вы будете перенаправлены на страницу `/schools`

### Создание новой школы

1. На странице `/schools` нажмите кнопку "➕ Добавить школу"
2. Заполните форму:
   - **Название школы** (обязательно)
   - **ФИО контактного лица** (опционально)
   - **Адрес** (опционально)
   - **Номер телефона** (опционально)
   - **Статус** (Активный/Заблокирован)
   - **Логин администратора школы** (обязательно)
   - **Пароль администратора школы** (обязательно)
   - **Функции** - выберите, какие функции включить для школы
3. Нажмите "Сохранить"
4. Система создаст:
   - Новую школу
   - Администратора школы с указанными логином и паролем
   - Пустые настройки клуба
   - Feature flags для выбранных функций

**Важно:** После создания школы сохраните логин и пароль администратора! Они будут показаны в модальном окне после создания.

### Редактирование школы

1. На странице списка школ нажмите "Редактировать" у нужной школы
2. Измените необходимые поля
3. Для изменения пароля администратора:
   - Введите новый пароль в поле "Пароль администратора"
   - Оставьте пустым, если не хотите менять пароль
4. Нажмите "Сохранить"

### Блокировка/разблокировка школы

1. На странице списка школ нажмите "Отключить" или "Включить"
2. Подтвердите действие
3. При блокировке школы:
   - Все пользователи школы не смогут войти в систему
   - Доступ к данным школы будет заблокирован

### Удаление школы

**⚠️ ВНИМАНИЕ: Это действие необратимо!**

При удалении школы удаляются:
- Все ученики школы
- Все группы
- Все пользователи школы
- Все платежи и финансовые данные
- Все настройки и конфигурации

1. На странице списка школ нажмите "🗑️ Удалить"
2. Подтвердите удаление дважды
3. Школа и все её данные будут удалены

## Изоляция данных

Система обеспечивает полную изоляцию данных между школами:

- Каждая школа видит только свои данные
- Пользователи школы не могут получить доступ к данным других школ
- Все запросы автоматически фильтруются по `school_id`
- Супер-администратор видит все данные для управления платформой

## Функции (Feature Flags)

При создании школы можно включить/выключить следующие функции:

- **Telegram бот** - интеграция с Telegram для уведомлений
- **Система вознаграждений** - система баллов и наград для учеников
- **Система карточек** - желтые/красные карточки для учеников
- **Распознавание лиц** - вход по распознаванию лица
- **Посещаемость** - отслеживание посещаемости занятий
- **Платежи** - система приема платежей
- **Финансы** - финансовый учет

## Вход администратора школы

1. Откройте страницу входа: `/login`
2. Введите логин и пароль администратора школы
3. После входа вы будете перенаправлены на главную страницу (`/dashboard`)
4. Администратор школы может управлять только своей школой

## Безопасность

- Все пароли хранятся в хешированном виде (bcrypt)
- Логины уникальны в рамках всей системы
- Супер-администратор не может войти как администратор школы (разные роли)
- При блокировке школы доступ блокируется немедленно

## API Endpoints

### Для супер-администратора

- `GET /api/schools` - получить список всех школ
- `GET /api/schools/<id>` - получить информацию о школе
- `POST /api/schools` - создать новую школу
- `PUT /api/schools/<id>` - обновить школу
- `POST /api/schools/<id>/delete` - удалить школу
- `POST /api/schools/switch` - переключиться на другую школу (для просмотра)

### Для администратора школы

Все стандартные endpoints автоматически фильтруются по `school_id`:
- `/api/students` - только ученики своей школы
- `/api/groups` - только группы своей школы
- `/api/payments` - только платежи своей школы
- И т.д.

## Структура базы данных

### Основные таблицы с изоляцией по школам:

- `schools` - школы (тенанты)
- `users` - пользователи (с `school_id`)
- `students` - ученики (с `school_id`)
- `groups` - группы (с `school_id`)
- `tariffs` - тарифы (с `school_id`)
- `payments` - платежи (через `students.school_id`)
- `attendance` - посещаемость (через `students.school_id`)
- `expenses` - расходы (с `school_id`)
- `club_settings` - настройки клуба (с `school_id`, один к одному)
- `school_features` - feature flags (с `school_id`)

## Миграция существующих данных

Если у вас уже есть данные без `school_id`, используйте скрипты миграции:

```bash
# Миграция существующих данных в первую школу
python migrate_to_saas.py
```

## Поддержка

При возникновении проблем проверьте:
1. Роль пользователя (должна быть `super_admin` для управления школами)
2. Логи в консоли браузера (F12)
3. Логи сервера

## Дополнительные скрипты

- `create_super_admin.py` - создать супер-администратора (интерактивно)
- `create_super_admin_auto.py` - создать супер-администратора (автоматически)
- `migrate_to_saas.py` - мигрировать существующие данные в SaaS-структуру

