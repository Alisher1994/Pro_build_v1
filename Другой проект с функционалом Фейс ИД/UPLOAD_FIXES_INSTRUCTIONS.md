# 📤 Инструкция по загрузке исправленных файлов

## 🚀 Быстрый запуск

1. **Откройте PowerShell** в папке проекта:
   ```powershell
   cd "C:\Users\LOQ\Desktop\App\CAM\football_school"
   ```

2. **Запустите скрипт:**
   ```powershell
   .\upload_fixes.ps1
   ```

3. **Введите пароль:** `12345678` (когда SCP попросит)

## 📋 Файлы, которые будут загружены:

1. ✅ `app.py` - Исправлена логика кассы (только наличные платежи)
2. ✅ `frontend/static/js/cash.js` - Исправлена подстановка отрицательного баланса
3. ✅ `frontend/static/js/students.js` - Исправлена функция openTransferPaymentModal
4. ✅ `frontend/templates/students.html` - Исправлен вызов closeTransferPaymentModal

## 🔧 После загрузки на сервере:

Выполните команды:

```bash
# Подключение
ssh root@45.92.173.37

# Переход в директорию
cd /opt/football_school

# Установка прав (если нужно)
chown -R www-data:www-data /opt/football_school
chmod -R 775 /opt/football_school/database
chmod -R 775 /opt/football_school/frontend/static/uploads

# Перезапуск сервиса
systemctl restart football_school

# Проверка статуса
systemctl status football_school --no-pager -l

# Просмотр логов (если есть ошибки)
journalctl -u football_school -n 50 --no-pager
```

## ✅ Что исправлено:

1. **Ошибка `openTransferPaymentModal is not defined`**
   - Функция теперь глобальная (`window.openTransferPaymentModal`)
   - Добавлены проверки наличия функции

2. **Кнопка "Передача" не работает**
   - Исправлен обработчик кнопки
   - Функция доступна глобально

3. **Проблема с кассой (отрицательный баланс)**
   - Касса учитывает только наличные платежи (`payment_type = 'cash'`)
   - Карточные платежи не учитываются в кассе
   - Отрицательный баланс не подставляется в поле суммы передачи

## 🔐 Пароль для SSH: `12345678`


