# 📤 Инструкция по загрузке исправления: неактивные нерабочие дни

## ✅ Что было добавлено:

### **Frontend (`frontend/static/js/groups.js`):**
- ✅ Нерабочие дни теперь неактивны (серые) и не могут быть выбраны
- ✅ Визуальное отличие нерабочих дней в заголовках и ячейках
- ✅ Проверка рабочих дней при выборе слотов (для создания и редактирования группы)

### **Frontend (`frontend/static/css/style.css`):**
- ✅ Добавлены стили для нерабочих дней (серый цвет, перечеркнутый вид)
- ✅ Стили для заголовков нерабочих дней

### Как это работает:
1. Система загружает настройки клуба (рабочие дни)
2. В визуализации расписания дни, которые не входят в рабочие дни, отображаются серым цветом
3. Нерабочие дни нельзя выбрать (нет обработчика клика)
4. Если в настройках выбраны, например, Пн, Ср, Пт, то остальные дни (Вт, Чт, Сб, Вс) будут неактивными

---

## 🚀 Быстрая загрузка:

### PowerShell (из любой директории):
```powershell
scp "C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\static\js\groups.js" root@45.92.173.37:/opt/football_school/frontend/static/js/groups.js
scp "C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\static\css\style.css" root@45.92.173.37:/opt/football_school/frontend/static/css/style.css
```

**Пароль:** `12345678`

---

## 🔧 После загрузки на сервере:

```bash
ssh root@45.92.173.37
systemctl restart football_school
```

---

## ✅ Проверка работы:

1. Откройте настройки клуба и выберите рабочие дни (например: Пн, Ср, Пт)
2. Сохраните настройки
3. Откройте страницу групп: `https://d-promo.uz/groups`
4. Нажмите "Добавить группу" или "Редактировать" у существующей группы
5. **Проверьте расписание:**
   - Нерабочие дни должны быть серыми
   - Нерабочие дни не должны реагировать на клик
   - На нерабочих днях должен быть текст "Не рабочий день"
   - Заголовки нерабочих дней тоже должны быть серыми

---

## 📋 Файлы для загрузки:

**Два файла:**
1. `frontend/static/js/groups.js` - логика блокировки нерабочих дней
2. `frontend/static/css/style.css` - стили для нерабочих дней

---

## 🔍 Детали изменений:

### JavaScript:
- `renderScheduleVisualization()` - проверяет рабочие дни при создании группы
- `renderEditScheduleVisualization()` - проверяет рабочие дни при редактировании группы
- `selectTimeSlot()` - блокирует выбор нерабочих дней
- `selectEditTimeSlot()` - блокирует выбор нерабочих дней

### CSS:
- `.schedule-slot.non-working` - стили для нерабочих ячеек (серый, перечеркнутый)
- `.schedule-header.non-working-day` - стили для заголовков нерабочих дней

---

## 📝 Примечания:

- Изменения применяются как при создании, так и при редактировании группы
- Нерабочие дни определяются из настроек клуба
- Если настройки клуба не загружены, все дни будут доступны (обратная совместимость)
- После загрузки обязательно перезапустите сервис `football_school`
- Очистите кэш браузера (Ctrl + Shift + R)

