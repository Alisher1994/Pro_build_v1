# Интеграция IFC моделей в ProBIM

## ✅ Реализованный функционал

### 1. Загрузка IFC моделей
- Каждый раздел сметы (этап) может иметь свою IFC модель
- Кнопка загрузки в заголовке каждого раздела
- Поддержка формата `.ifc`
- Автоматическое сохранение в `backend/uploads/ifc/`

### 2. 3D Просмотр (xeokit viewer)
- Canvas для отображения 3D модели
- Навигация по модели (вращение, зум, панорамирование)
- Выбор элементов кликом
- Множественный выбор (Ctrl + Click)
- Панель управления (Fit to View, Reset Colors)

### 3. Свойства элементов
- Отображение типа IFC (IfcWall, IfcSlab и т.д.)
- ID элемента и GUID
- Геометрические свойства (из метаданных)
- Панель свойств внизу справа

### 4. Связывание элементов с ресурсами
- Иконка состояния связи в каждом ресурсе
  - 🔗 Зелёная = связано
  - ⛓️‍💥 Серая = не связано
- Выбор ресурсов (Ctrl + Click)
- Выбор элементов в 3D модели (Ctrl + Click)
- Кнопки "Связать" / "Отвязать" в заголовке
- Сохранение связей в базе данных

### 5. Визуализация связей
- Зелёная подсветка связанных элементов
- Автоматическая подсветка при загрузке модели
- Возможность изолировать элементы

## 📋 Как использовать

### Шаг 1: Загрузить IFC для раздела

```javascript
1. Откройте смету
2. Найдите раздел (этап) в дереве
3. Нажмите кнопку 📦 рядом с названием раздела
4. Выберите .ifc файл
5. Модель загрузится в панель справа
```

### Шаг 2: Навигация по 3D модели

**Управление камерой:**
- **Вращение**: Левая кнопка мыши + движение
- **Панорамирование**: Правая кнопка мыши + движение
- **Зум**: Колесико мыши
- **Fit to View**: Кнопка □ в правом верхнем углу

### Шаг 3: Выбор элементов IFC

```javascript
1. Кликните по элементу в 3D модели
   - Элемент выделится оранжевым контуром
   - Свойства отобразятся в панели внизу

2. Множественный выбор:
   - Зажмите Ctrl (или Cmd на Mac)
   - Кликайте по элементам
   - Все выбранные подсвечиваются
```

### Шаг 4: Выбор ресурсов для связывания

```javascript
1. В дереве слева найдите ресурс
2. Зажмите Ctrl + кликните по строке ресурса
3. Строка подсветится голубым
4. Можно выбрать несколько ресурсов
```

### Шаг 5: Связать ресурсы с элементами IFC

```javascript
1. Выберите ресурсы (Ctrl + Click на ресурсах)
2. Выберите элементы IFC (Ctrl + Click в 3D модели)
3. Появятся кнопки "Связать" / "Отвязать"
4. Нажмите "Связать"
5. Элементы станут зелёными ✅
6. Иконка связи изменится на 🔗
```

### Шаг 6: Отвязать (если нужно)

```javascript
1. Выберите ресурсы с иконкой 🔗
2. Нажмите кнопку "Отвязать"
3. Подтвердите действие
4. Связь удалится, иконка станет серой ⛓️‍💥
```

## 🔧 Технические детали

### База данных

**Модель WorkType** (используется как ресурс):
```prisma
model WorkType {
  id            String   @id @default(uuid())
  stageId       String
  name          String
  unit          String?
  quantity      Float?
  unitCost      Float?
  totalCost     Float    @default(0)
  
  // IFC связи
  ifcElements   String?  // JSON массив ["id1", "id2", ...]
  ifcProperties String?  // JSON свойства элементов
  
  description   String?  // Используется для типа ресурса
  // ...
}
```

**Модель EstimateSection** (раздел с IFC):
```prisma
model EstimateSection {
  id          String   @id @default(uuid())
  estimateId  String
  code        String
  name        String
  ifcFileUrl  String?  // Путь к IFC файлу
  xktFileUrl  String?  // Путь к XKT файлу (для будущего)
  // ...
}
```

### API Endpoints

**Загрузка IFC:**
```http
POST /api/sections/:sectionId/upload-ifc
Content-Type: multipart/form-data

file: <.ifc file>
```

**Связывание ресурса:**
```http
PUT /api/work-types/:resourceId
Content-Type: application/json

{
  "ifcElements": ["element-id-1", "element-id-2"],
  "ifcProperties": "{...}"
}
```

**Отвязывание:**
```http
PUT /api/work-types/:resourceId
Content-Type: application/json

{
  "ifcElements": null
}
```

### Frontend модули

**IFCViewerManager** (`js/ifcViewer.js`):
- `init()` - Инициализация viewer
- `loadXKT()` - Загрузка XKT модели
- `handleElementClick()` - Обработка выбора
- `highlightElements()` - Подсветка элементов
- `getElementProperties()` - Получение свойств

**EstimateManager** (`js/estimate.js`):
- `uploadIFCForSection()` - Загрузка IFC
- `loadIfcViewer()` - Загрузка модели в viewer
- `handleIfcElementSelected()` - Обработка выбора
- `linkSelectedResource()` - Связывание
- `unlinkSelectedResource()` - Отвязывание

## 🚀 Будущие улучшения

### 1. Конвертация IFC → XKT
```bash
# На backend нужно добавить
npm install @xeokit/xeokit-convert

# Конвертация при загрузке
xeokit-convert input.ifc output.xkt
```

### 2. Автоматический расчёт количества
```javascript
// Из свойств IFC элемента
const volume = element.properties.NetVolume;
const area = element.properties.NetArea;
const length = element.properties.Length;

// Автоматически заполнять quantity в ресурсе
```

### 3. Фильтрация элементов
```javascript
// По типу IFC
IFCViewerManager.getElementsByType('IfcWall');

// По связанным ресурсам
EstimateManager.filterLinkedElements();
```

### 4. Экспорт отчётов
```javascript
// Экспорт связей ресурсов с элементами
EstimateManager.exportResourceLinks('excel');
```

### 5. Визуализация прогресса
```javascript
// Раскрасить элементы по статусу
- Зелёный = связано и утверждено
- Жёлтый = связано, ожидает проверки
- Красный = не связано
- Серый = скрыто
```

## 📚 Документация xeokit

- [xeokit SDK](https://xeokit.io/docs/sdk/)
- [XKTLoaderPlugin](https://xeokit.io/docs/sdk/plugins/XKTLoaderPlugin/)
- [Примеры](https://xeokit.github.io/xeokit-sdk/examples/)

## 🐛 Известные ограничения

1. **Формат XKT**: Viewer работает только с XKT, нужна конвертация IFC
2. **Размер файлов**: Большие IFC модели могут грузиться долго
3. **Свойства IFC**: Не все свойства автоматически извлекаются
4. **Множественные модели**: Пока можно загрузить только одну модель за раз

## ✅ Тестирование

### Тестовые IFC файлы:
- [buildingSMART Samples](https://github.com/buildingSMART/Sample-Test-Files)
- [KIT IFC Examples](https://www.ifcwiki.org/index.php?title=KIT_IFC_Examples)

### Проверка функционала:
1. ✅ Загрузка IFC файла
2. ✅ Отображение в 3D viewer
3. ✅ Выбор элементов
4. ✅ Отображение свойств
5. ✅ Связывание с ресурсами
6. ✅ Подсветка связанных элементов
7. ✅ Отвязывание
8. ✅ Сохранение в БД

## 🎯 Следующие шаги

1. ⏳ Установить xeokit-convert на backend
2. ⏳ Добавить автоматическую конвертацию IFC → XKT
3. ⏳ Реализовать извлечение количества из IFC
4. ⏳ Добавить режимы визуализации (по типу, по статусу)
5. ⏳ Экспорт связей в Excel
