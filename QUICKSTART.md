# 🚀 Быстрый старт ProBIM

## Backend уже запущен ✅

Backend сервер работает на http://localhost:3001

## Запуск Frontend

### Вариант 1: Простой (рекомендуется)

Просто откройте файл в браузере:
```
C:\Users\LOQ\Desktop\App\Pro_build\ProBIM\frontend\index.html
```

Дважды кликните на файл `index.html` - он откроется в браузере по умолчанию.

### Вариант 2: С HTTP сервером (если Вариант 1 не работает)

Если браузер блокирует CORS запросы при открытии через file://, используйте HTTP сервер:

#### Используя Python:
```powershell
cd C:\Users\LOQ\Desktop\App\Pro_build\ProBIM\frontend
python -m http.server 8000
```

Затем откройте http://localhost:8000

#### Или используя батник:
Дважды кликните на файл `start-frontend.bat`

### Вариант 3: VS Code Live Server

1. Откройте папку `ProBIM/frontend` в VS Code
2. Кликните правой кнопкой на `index.html`
3. Выберите "Open with Live Server"

## Что делать дальше

1. **Создайте первый проект** - нажмите кнопку "+" в левом сайдбаре
2. **Добавьте блок** - через Ribbon меню → Смета → Добавить блок
3. **Создайте смету** - кликните на блок и добавьте смету
4. **Добавьте разделы** - АР, КЖ, ОВ и т.д.

## Возможные проблемы

### Ошибка: "Failed to fetch"
- Убедитесь, что backend запущен (проверьте http://localhost:3001/api/health)
- Если backend не запущен, выполните:
  ```powershell
  cd C:\Users\LOQ\Desktop\App\Pro_build\ProBIM\backend
  npm run dev
  ```

### Ошибка CORS
- Используйте HTTP сервер (Вариант 2 или 3) вместо прямого открытия файла

### Белый экран
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что все файлы JS загружены корректно

## API Endpoints для тестирования

Откройте в браузере для проверки:

- http://localhost:3001/api/health - проверка работы сервера
- http://localhost:3001/api/projects - список проектов
- http://localhost:3001/api/blocks - список блоков

## Структура приложения

```
ProBIM/
├── backend/ (запущен на :3001)
│   └── src/
│       └── routes/  - API endpoints
└── frontend/ (откройте index.html)
    ├── index.html   - главная страница
    ├── css/
    │   └── style.css
    └── js/
        ├── api.js   - API клиент
        ├── ui.js    - UI компоненты
        ├── estimate.js - модуль смет
        └── app.js   - главное приложение
```

Приятной работы! 🎉
