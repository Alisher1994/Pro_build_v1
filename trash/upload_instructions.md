# 📤 Инструкция по загрузке проекта на сервер

## ⚠️ Важно: Что НЕ нужно загружать

**НЕ загружайте эти папки/файлы:**
- ❌ `venv/` - виртуальное окружение (создаётся на сервере)
- ❌ `__pycache__/` - кэш Python
- ❌ `database/*.db` - локальная база данных
- ❌ `.git/` - папка Git (если не используете Git на сервере)

## ✅ Способ 1: WinSCP с фильтром (рекомендуется)

### Настройка фильтра в WinSCP:

1. **Откройте настройки WinSCP:**
   - Нажмите `F9` или меню "Параметры" → "Настройки"

2. **Перейдите в фильтры:**
   - В левом меню: "Панели" → "Фильтр"

3. **Добавьте исключения:**
   Нажмите "Добавить" и добавьте каждый элемент:
   - `venv` → Тип: "Исключить"
   - `__pycache__` → Тип: "Исключить"
   - `*.db` → Тип: "Исключить"
   - `.git` → Тип: "Исключить"

4. **Примените фильтр:**
   - Нажмите "OK"
   - Теперь при загрузке эти файлы будут автоматически пропускаться

5. **Загрузите проект:**
   - Перетащите папку `football_school` в `/opt/` на сервере
   - WinSCP автоматически пропустит исключённые файлы

## ✅ Способ 2: Ручной выбор файлов

1. **Откройте папку проекта в левой панели WinSCP**

2. **Выделите все файлы и папки, КРОМЕ:**
   - `venv` (НЕ выделяйте!)
   - `__pycache__` (НЕ выделяйте!)
   - `database/*.db` (НЕ выделяйте!)

3. **Перетащите выделенное в `/opt/` на сервере**

## ✅ Способ 3: PowerShell скрипт (автоматический)

Запустите скрипт `upload_to_server.ps1`:

```powershell
cd C:\Users\LOQ\Desktop\App\CAM\football_school
.\upload_to_server.ps1
```

Скрипт автоматически:
- ✅ Исключит ненужные файлы
- ✅ Загрузит проект на сервер
- ✅ Покажет следующие шаги

## ✅ Способ 4: Через Git (если проект на GitHub)

Если ваш проект уже на GitHub:

```bash
# На сервере
ssh root@45.92.173.37
cd /opt
git clone https://github.com/ваш-репозиторий/football_school.git
cd football_school
```

## 🔍 Проверка после загрузки

После загрузки проверьте на сервере:

```bash
ssh root@45.92.173.37
cd /opt/football_school
ls -la

# Должны быть видны:
# - app.py
# - requirements.txt
# - frontend/
# - backend/
# НО НЕ должно быть:
# - venv/ (если видите - удалите: rm -rf venv)
```

## 🚀 После успешной загрузки

```bash
cd /opt/football_school
chmod +x setup_vps.sh
./setup_vps.sh
```

