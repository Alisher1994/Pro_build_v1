# ProBIM - Профессиональная система управления строительными проектами

## Описание

ProBIM - это комплексная система управления строительными проектами, включающая:

- **Управление проектами** - создание и управление объектами
- **Блоки и сметы** - иерархическая структура объектов
- **Разделы смет** - АР, КЖ, КМ, ОВ, ВК и другие разделы
- **Этапы и виды работ** - детализация работ
- **Ресурсы** - материалы, работы, оборудование
- **IFC модели** - загрузка и привязка BIM моделей
- **График производства работ (ГПР)**
- **Снабжение** - планирование поставок
- **Финансы** - контроль бюджета проекта

## Архитектура

### Backend
- Node.js + Express
- TypeScript
- Prisma ORM
- SQLite база данных

### Frontend
- Vanilla JavaScript
- MS Office Ribbon интерфейс
- Soft дизайн на основе Fluent Design

## Иерархия данных

```
Проект (Project)
  └─ Блок (Block) - здание/корпус
      └─ Смета (Estimate)
          └─ Раздел (EstimateSection) - АР, КЖ, ОВ и т.д.
              ├─ IFC файл
              └─ Этап (EstimateStage)
                  └─ Вид работ (WorkType)
                      └─ Ресурс (Resource)
                          ├─ Материалы
                          ├─ Работы
                          └─ Оборудование
```

## Установка и запуск

### 1. Установка зависимостей Backend

```powershell
cd C:\Users\LOQ\Desktop\App\Pro_build\ProBIM\backend
npm install
```

### 2. Настройка базы данных

```powershell
# Создание таблиц базы данных
npx prisma migrate dev --name init

# Генерация Prisma Client
npx prisma generate
```

### 3. Запуск Backend сервера

```powershell
# Development режим
npm run dev

# Production режим
npm run build
npm start
```

Backend будет доступен на http://localhost:3001

### 4. Запуск Frontend

Откройте файл `frontend/index.html` в браузере или используйте локальный сервер:

```powershell
# Если у вас установлен Python
cd C:\Users\LOQ\Desktop\App\Pro_build\ProBIM\frontend
python -m http.server 8000

# Или используйте VS Code Live Server
```

Frontend будет доступен на http://localhost:8000

## API Endpoints

### Проекты
- `GET /api/projects` - получить все проекты
- `POST /api/projects` - создать проект
- `GET /api/projects/:id` - получить проект
- `PUT /api/projects/:id` - обновить проект
- `DELETE /api/projects/:id` - удалить проект

### Блоки
- `GET /api/blocks?projectId=...` - получить блоки
- `POST /api/blocks` - создать блок
- `PUT /api/blocks/:id` - обновить блок
- `DELETE /api/blocks/:id` - удалить блок

### Сметы
- `GET /api/estimates?projectId=...&blockId=...` - получить сметы
- `POST /api/estimates` - создать смету
- `PUT /api/estimates/:id` - обновить смету
- `DELETE /api/estimates/:id` - удалить смету

### Разделы
- `GET /api/sections?estimateId=...` - получить разделы
- `POST /api/sections` - создать раздел
- `POST /api/sections/:id/upload-ifc` - загрузить IFC файл
- `DELETE /api/sections/:id` - удалить раздел

### Этапы
- `GET /api/stages?sectionId=...` - получить этапы
- `POST /api/stages` - создать этап

### Виды работ
- `GET /api/work-types?stageId=...` - получить виды работ
- `POST /api/work-types` - создать вид работ

### Ресурсы
- `GET /api/resources?workTypeId=...` - получить ресурсы
- `POST /api/resources` - создать ресурс
- `POST /api/resources/:id/link-ifc` - привязать элементы IFC

### График (ГПР)
- `GET /api/schedules?projectId=...` - получить графики
- `POST /api/schedules` - создать график
- `POST /api/schedules/:id/items` - добавить позицию в график

### Снабжение
- `GET /api/supplies?projectId=...` - получить планы снабжения
- `POST /api/supplies` - создать план снабжения
- `POST /api/supplies/:id/items` - добавить позицию снабжения

### Финансы
- `GET /api/finances?projectId=...` - получить финансовые операции
- `POST /api/finances` - создать операцию
- `GET /api/finances/project/:id/summary` - получить сводку

## Основные возможности

### ✅ Реализовано

1. **Управление проектами**
   - Создание, редактирование, удаление проектов
   - Список всех проектов

2. **Блоки**
   - Создание блоков (зданий/корпусов)
   - Указание этажности и площади

3. **Сметы**
   - Создание смет для блоков
   - Иерархическая структура

4. **Разделы смет**
   - АР, КЖ, КМ, ОВ, ВК и другие
   - Загрузка IFC файлов для каждого раздела

5. **Этапы, Виды работ, Ресурсы**
   - Полная иерархия сметы
   - CRUD операции для всех уровней

6. **Ribbon интерфейс**
   - MS Office стиль меню
   - Soft дизайн

7. **REST API**
   - Полный набор endpoints
   - Поддержка всех операций

### 🚧 В разработке

1. **IFC Viewer** - 3D просмотр моделей
2. **График производства работ** - диаграмма Ганта
3. **Снабжение** - расширенный функционал
4. **Аналитика** - графики и отчеты
5. **Экспорт** - в Excel, PDF

## Структура проекта

```
ProBIM/
├── backend/
│   ├── prisma/
│   │   ├── schema.prisma      # Схема базы данных
│   │   └── dev.db             # SQLite база данных
│   ├── src/
│   │   ├── server.ts          # Express сервер
│   │   └── routes/            # API маршруты
│   │       ├── projects.ts
│   │       ├── blocks.ts
│   │       ├── estimates.ts
│   │       ├── sections.ts
│   │       ├── stages.ts
│   │       ├── workTypes.ts
│   │       ├── resources.ts
│   │       ├── schedules.ts
│   │       ├── supplies.ts
│   │       └── finances.ts
│   ├── package.json
│   └── tsconfig.json
├── frontend/
│   ├── index.html             # Главная страница
│   ├── css/
│   │   └── style.css          # Стили
│   └── js/
│       ├── api.js             # API клиент
│       ├── ui.js              # UI утилиты
│       ├── estimate.js        # Модуль смет
│       └── app.js             # Главное приложение
└── README.md
```

## База данных

### Основные модели

- **Project** - проекты
- **Block** - блоки/здания
- **Estimate** - сметы
- **EstimateSection** - разделы смет (АР, КЖ и т.д.)
- **EstimateStage** - этапы работ
- **WorkType** - виды работ
- **Resource** - ресурсы (материалы, работы, оборудование)
- **Schedule** - графики работ
- **ScheduleItem** - позиции графика
- **Supply** - планы снабжения
- **SupplyItem** - позиции снабжения
- **Finance** - финансовые операции
- **IFCElement** - элементы IFC моделей

## Технологии

- **Backend**: Node.js, Express, TypeScript, Prisma, SQLite
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Дизайн**: Microsoft Fluent Design, Office Ribbon UI
- **API**: RESTful
- **БД**: SQLite (может быть заменена на PostgreSQL/MySQL)

## Автор

Разработано на основе анализа проектов:
- MyBuildingPro (стили и UI)
- Новая папка (архитектура и функционал)

## Лицензия

Все права защищены
