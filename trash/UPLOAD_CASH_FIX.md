# 📤 Загрузка исправлений логики кассы на сервер

## 🎯 Измененные файлы:

1. **`app.py`** - Исправлена логика расчета остатка кассы:
   - Теперь учитываются только наличные платежи (`payment_type = 'cash'`)
   - Добавлена проверка достаточности средств при создании передачи
   - Добавлена проверка достаточности средств при обновлении передачи

## 📋 Инструкция по загрузке:

### Вариант 1: Через WinSCP (рекомендуется)

1. **Подключитесь к серверу через WinSCP:**
   - Хост: `45.92.173.37`
   - Порт: `22`
   - Пользователь: `root`
   - Пароль: `12345678`

2. **Загрузите файл:**
   - Перейдите в `/opt/football_school/`
   - Скопируйте файл:
     - `app.py` → `/opt/football_school/app.py`

### Вариант 2: Через PowerShell (SCP команды)

```powershell
# Перейдите в директорию проекта
cd C:\Users\LOQ\Desktop\App\CAM\football_school

# Загрузить обновленный файл
scp app.py root@45.92.173.37:/opt/football_school/
```

## 🔄 После загрузки файла на VPS:

### 1. Подключитесь к серверу по SSH:
```bash
ssh root@45.92.173.37
```

### 2. Перейдите в директорию проекта:
```bash
cd /opt/football_school
```

### 3. Установите правильные права:
```bash
chown www-data:www-data /opt/football_school/app.py
chmod 644 /opt/football_school/app.py
```

### 4. Перезапустите сервис:
```bash
systemctl restart football_school
```

### 5. Проверьте статус:
```bash
systemctl status football_school --no-pager -l
```

### 6. Проверьте логи (если есть ошибки):
```bash
journalctl -u football_school -n 50 --no-pager
```

## ✅ Проверка работы:

После перезапуска проверьте:

1. **Страница кассы:**
   - `https://d-promo.uz/finances` → вкладка "Касса"

2. **Ожидаемый результат:**
   - ✅ Остаток кассы рассчитывается только на основе наличных платежей
   - ✅ Нельзя передать больше, чем есть в кассе
   - ✅ Если прихода нет, остаток будет 0 (не отрицательным)

## 🚨 Если что-то не работает:

1. **Проверьте права доступа:**
   ```bash
   ls -la /opt/football_school/app.py
   ```

2. **Проверьте логи:**
   ```bash
   tail -f /var/log/football_school/error.log
   journalctl -u football_school -f
   ```

3. **Проверьте синтаксис Python:**
   ```bash
   python3 -m py_compile /opt/football_school/app.py
   ```

4. **Перезагрузите Nginx:**
   ```bash
   systemctl reload nginx
   ```

5. **Перезапустите приложение:**
   ```bash
   systemctl restart football_school
   ```

## 📝 Что было исправлено:

1. **Расчет остатка кассы:**
   - Раньше: учитывались ВСЕ платежи (наличные, карта, click, payme, uzum)
   - Теперь: учитываются ТОЛЬКО наличные платежи (`payment_type = 'cash'`)

2. **Проверка при создании передачи:**
   - Добавлена проверка: нельзя передать больше, чем есть в кассе
   - Если остаток <= 0, передача блокируется

3. **Проверка при обновлении передачи:**
   - При изменении суммы проверяется достаточность средств
   - Учитывается, что текущая передача уже учтена в балансе


