# 📤 Инструкция по загрузке исправления: стили выбранных слотов при редактировании

## 🐛 Исправленные проблемы:

1. **Стили выбранных слотов при редактировании группы:**
   - ❌ Было: отображались без стиля, в столбик, как Windows XP
   - ✅ Стало: красивое отображение в ряд, как при создании группы

2. **Интервалы длительности занятий:**
   - ❌ Было: неполный список, до 4 часов
   - ✅ Стало: интервалы по 15 минут от 30 минут до 5 часов

---

## ✅ Что было исправлено:

### **Frontend (`frontend/static/js/groups.js`):**
- ✅ Исправлена функция `updateEditSelectedSlotsDisplay()` - теперь использует те же классы и структуру, что и `updateSelectedSlotsDisplay()`
- ✅ Выбранные слоты теперь отображаются в ряд с красивыми синими бейджами

### **Frontend (`frontend/templates/groups.html`):**
- ✅ Обновлены селекты длительности занятий (для создания и редактирования)
- ✅ Добавлены все интервалы по 15 минут: 30, 45, 1 час, 1:15, 1:30, 1:45, 2, 2:15, 2:30, 2:45, 3, 3:15, 3:30, 3:45, 4, 4:15, 4:30, 4:45, 5 часов

---

## 🚀 Быстрая загрузка:

### PowerShell (из любой директории):
```powershell
scp "C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\static\js\groups.js" root@45.92.173.37:/opt/football_school/frontend/static/js/groups.js
scp "C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\templates\groups.html" root@45.92.173.37:/opt/football_school/frontend/templates/groups.html
```

**Пароль:** `12345678`

---

## 🔧 После загрузки на сервере:

```bash
ssh root@45.92.173.37
systemctl restart football_school
```

---

## ✅ Проверка работы:

1. **Проверка стилей выбранных слотов:**
   - Откройте страницу групп: `https://d-promo.uz/groups`
   - Нажмите "Редактировать" у любой группы
   - Выберите дни и время в расписании
   - **Выбранные слоты должны отображаться красиво в ряд с синими бейджами** (как при создании группы)

2. **Проверка интервалов длительности:**
   - Откройте форму создания или редактирования группы
   - Откройте селект "Длительность занятия"
   - **Должны быть все интервалы:** 30 минут, 45 минут, 1 час, 1:15, 1:30, 1:45, 2 часа, 2:15, 2:30, 2:45, 3 часа, 3:15, 3:30, 3:45, 4 часа, 4:15, 4:30, 4:45, 5 часов

---

## 📋 Файлы для загрузки:

**Два файла:**
1. `frontend/static/js/groups.js` - исправление функции отображения слотов
2. `frontend/templates/groups.html` - обновление селектов длительности

---

## 🔍 Детали исправлений:

### JavaScript (groups.js):
**До:**
```javascript
function updateEditSelectedSlotsDisplay() {
    // Использовались классы slot-badge, slot-badge-text, slot-badge-remove
    // Структура была другой
}
```

**После:**
```javascript
function updateEditSelectedSlotsDisplay() {
    // Теперь использует те же классы, что и updateSelectedSlotsDisplay():
    // selected-slot-badge и remove-slot
    // Та же структура HTML
}
```

### HTML (groups.html):
**Добавлены интервалы:**
- 4:15 (255 минут)
- 4:30 (270 минут) 
- 4:45 (285 минут)
- 5 часов (300 минут)

**Формат отображения изменен:**
- Было: "1 час 15 минут"
- Стало: "1:15" (более компактно)

---

## 📝 Примечания:

- Стили теперь одинаковые для создания и редактирования группы
- Интервалы длительности одинаковые в обеих формах
- После загрузки обязательно перезапустите сервис `football_school`
- Очистите кэш браузера (Ctrl + Shift + R)

