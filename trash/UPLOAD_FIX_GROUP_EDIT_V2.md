# 📤 Инструкция по загрузке исправления редактирования группы (v2)

## 🐛 Исправленная ошибка:
**400 (BAD REQUEST) при редактировании группы - groups.js:719**

## ✅ Что было исправлено (v2):

### **Frontend (`frontend/static/js/groups.js`):**
- ✅ **Исправлена обработка `schedule_time`** - теперь правильно обрабатывается случай, когда выбраны разные времена для разных дней (JSON формат)
- ✅ Добавлена валидация `schedule_time` перед отправкой
- ✅ Улучшена обработка ошибок - теперь показывается конкретное сообщение от сервера
- ✅ Добавлено логирование для отладки

### Проблема была в том, что:
Когда пользователь выбирал разные времена для разных дней, `schedule_time` отправлялся как JSON строка (например: `{"1":"17:00","4":"18:00"}`), а сервер ожидал формат `"HH:MM"` (например: `"17:00"`).

### Решение:
Теперь если `schedule_time` это JSON, мы берем первое время из выбранных слотов и отправляем его в правильном формате.

---

## 🚀 Быстрая загрузка:

### PowerShell (из любой директории):
```powershell
scp "C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\static\js\groups.js" root@45.92.173.37:/opt/football_school/frontend/static/js/groups.js
```

**Пароль:** `12345678`

---

## 🔧 После загрузки на сервере:

```bash
ssh root@45.92.173.37
systemctl restart football_school
```

---

## ✅ Проверка работы:

1. Откройте страницу групп: `https://d-promo.uz/groups`
2. Нажмите "Редактировать" у любой группы
3. Измените параметры и нажмите "Сохранить"
4. **Не должно быть ошибок 400 в консоли**
5. Группа должна успешно обновиться

---

## 📋 Файл для загрузки:

**Только один файл:**
- `frontend/static/js/groups.js`

---

## 🔍 Детали исправления:

### До исправления:
```javascript
schedule_time: scheduleTimeValue || null,  // Могло быть JSON строкой
```

### После исправления:
```javascript
// Проверяем, не является ли это JSON
let scheduleTime = null;
if (scheduleTimeValue && scheduleTimeValue.trim() !== '') {
    try {
        const timeMap = JSON.parse(scheduleTimeValue);
        // Если это объект, берем первое время из выбранных слотов
        if (editSelectedSlots.length > 0) {
            scheduleTime = editSelectedSlots[0].time;
        }
    } catch (e) {
        // Если не JSON, значит это обычное время в формате HH:MM
        scheduleTime = scheduleTimeValue;
    }
}
```

---

## 📝 Примечания:

- Исправлен только один файл: `groups.js`
- Изменение минимальное, но критичное
- После загрузки обязательно перезапустите сервис `football_school`
- Очистите кэш браузера (Ctrl + Shift + R)

