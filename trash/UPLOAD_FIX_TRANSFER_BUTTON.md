# 📤 Инструкция по загрузке исправления кнопки перевода

## 🐛 Исправленная ошибка:
**Uncaught ReferenceError: openTransferPaymentModal is not defined**

## ✅ Что было исправлено:
В файле `frontend/templates/students.html` на строке 4923:
- ❌ Было: `openTransferPaymentModal(paymentId, studentId, amount);`
- ✅ Стало: `window.openTransferPaymentModal(paymentId, studentId, amount);` с проверкой существования функции

---

## 🚀 Способ 1: Автоматическая загрузка (PowerShell)

### Windows:
```powershell
# Откройте PowerShell в папке проекта
cd "C:\Users\LOQ\Desktop\App\CAM\football_school"

# Запустите скрипт
.\upload_fix_transfer_button.ps1
```

### Linux/Mac:
```bash
# Сделайте скрипт исполняемым
chmod +x upload_fix_transfer_button.sh

# Запустите скрипт
./upload_fix_transfer_button.sh
```

---

## 🚀 Способ 2: Ручная загрузка через SCP

### Windows (PowerShell):
```powershell
scp frontend\templates\students.html root@45.92.173.37:/opt/football_school/frontend/templates/students.html
```

### Linux/Mac:
```bash
scp frontend/templates/students.html root@45.92.173.37:/opt/football_school/frontend/templates/students.html
```

**Пароль:** `12345678`

---

## 🚀 Способ 3: Ручная загрузка через WinSCP

1. **Откройте WinSCP**
2. **Подключитесь к серверу:**
   - Хост: `45.92.173.37`
   - Пользователь: `root`
   - Пароль: `12345678`
   - Порт: `22`

3. **Перейдите в папку:**
   ```
   /opt/football_school/frontend/templates/
   ```

4. **Загрузите файл:**
   - Локальный путь: `C:\Users\LOQ\Desktop\App\CAM\football_school\frontend\templates\students.html`
   - Удаленный путь: `/opt/football_school/frontend/templates/students.html`
   - Перезапишите существующий файл

---

## 🔧 После загрузки на сервере:

### Подключитесь по SSH:
```bash
ssh root@45.92.173.37
```

### Выполните команды:
```bash
# Переход в директорию проекта
cd /opt/football_school

# Установка прав доступа (если нужно)
chown -R www-data:www-data frontend/templates/students.html
chmod 644 frontend/templates/students.html

# Перезапуск сервиса
systemctl restart football_school

# Проверка статуса
systemctl status football_school --no-pager -l
```

### Проверка логов (если есть ошибки):
```bash
journalctl -u football_school -n 50 --no-pager
```

---

## ✅ Проверка работы:

1. **Откройте страницу учеников:**
   - `https://d-promo.uz/students`

2. **Проверьте кнопку перевода:**
   - Откройте карточку ученика
   - Найдите оплату в списке
   - Нажмите кнопку "Перевести" (иконка с двумя стрелками)
   - Модальное окно должно открыться без ошибок

3. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку Console
   - Не должно быть ошибки `openTransferPaymentModal is not defined`

---

## 📋 Файл для загрузки:

**Только один файл:**
- `frontend/templates/students.html`

---

## 🐛 Если что-то пошло не так:

1. **Проверьте подключение к серверу:**
   ```bash
   ssh root@45.92.173.37
   ```

2. **Проверьте права доступа:**
   ```bash
   ls -la /opt/football_school/frontend/templates/students.html
   ```

3. **Проверьте логи сервиса:**
   ```bash
   journalctl -u football_school -n 100 --no-pager
   ```

4. **Перезапустите Nginx:**
   ```bash
   systemctl reload nginx
   ```

5. **Очистите кэш браузера:**
   - Нажмите `Ctrl + Shift + R` (жесткая перезагрузка)
   - Или откройте в режиме инкогнито

---

## 📝 Примечания:

- Исправлен только один файл: `students.html`
- Изменение минимальное - добавлена проверка и правильный вызов функции
- После загрузки обязательно перезапустите сервис `football_school`
- Очистите кэш браузера для применения изменений

