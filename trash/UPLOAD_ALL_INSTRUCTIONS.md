# 📤 Инструкция по полной загрузке проекта на сервер

## 🚀 Быстрый старт

1. **Откройте PowerShell** в папке проекта:
   ```powershell
   cd "C:\Users\LOQ\Desktop\App\CAM\football_school"
   ```

2. **Запустите скрипт загрузки:**
   ```powershell
   .\upload_all_to_server.ps1
   ```

3. **Введите пароль при запросе:** `12345678`

4. **Подтвердите загрузку:** введите `y` когда скрипт спросит подтверждение

## 📋 Что делает скрипт:

- ✅ Копирует все файлы проекта во временную папку
- ✅ Исключает ненужные файлы:
  - `venv/` - виртуальное окружение Python
  - `__pycache__/` - кэш Python
  - `.git/` - репозиторий Git
  - `*.db` - файлы баз данных
  - `.pyc`, `.pyo`, `.pyd` - скомпилированные файлы Python
  - `node_modules/` - зависимости Node.js
  - Системные папки Windows
  - Вложенная папка `football_school/`
- ✅ Загружает все файлы на сервер через SCP
- ✅ Перезаписывает существующие файлы на сервере

## ⚠️ Важно:

1. **Пароль для SSH:** `12345678`
2. **Сервер:** `45.92.173.37`
3. **Путь на сервере:** `/opt/football_school`
4. **Все файлы на сервере будут перезаписаны!**

## 🔧 После загрузки на сервере:

Подключитесь по SSH и выполните команды:

```bash
# Подключение
ssh root@45.92.173.37

# Переход в директорию проекта
cd /opt/football_school

# Установка прав доступа
chown -R www-data:www-data /opt/football_school
chmod -R 775 /opt/football_school/database
chmod -R 775 /opt/football_school/frontend/static/uploads

# Перезапуск сервиса
systemctl restart football_school

# Проверка статуса
systemctl status football_school --no-pager -l

# Просмотр логов (если есть ошибки)
journalctl -u football_school -n 50 --no-pager
```

## 📊 Статистика загрузки:

Скрипт покажет:
- Общее количество файлов
- Количество скопированных файлов
- Количество исключенных файлов

## 🐛 Если что-то пошло не так:

1. **Проверьте подключение к серверу:**
   ```powershell
   ssh root@45.92.173.37
   ```

2. **Проверьте права доступа на сервере:**
   ```bash
   ls -la /opt/football_school
   ```

3. **Проверьте логи сервиса:**
   ```bash
   journalctl -u football_school -n 100 --no-pager
   ```

4. **Перезапустите Nginx:**
   ```bash
   systemctl reload nginx
   ```

## ✅ Проверка работы:

После загрузки проверьте работу сайта:
- Главная страница: `https://d-promo.uz`
- Вход в систему: `https://d-promo.uz/login`
- Мобильные страницы: `https://d-promo.uz/mobile-*`

## 📝 Примечания:

- Скрипт создает временную папку в `%TEMP%` и автоматически удаляет её после загрузки
- Если загрузка прервется, временная папка останется (можно удалить вручную)
- Время загрузки зависит от количества файлов и скорости интернета


